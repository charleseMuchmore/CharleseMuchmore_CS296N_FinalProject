@model AppUser
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    
        @if (Model == null)
        {           
            <h1 class="display-4">Welcome!</h1>
        } else
        {
            <h1 class="display-4">Welcome, <span>@Model.UserName</span>!</h1>
        }
</div>

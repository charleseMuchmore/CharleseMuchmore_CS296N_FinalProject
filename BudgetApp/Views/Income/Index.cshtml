@model List<Income>
@{
    ViewData["Title"] = "Income Page";
}

<a class="btn btn-primary" href="Income/AddIncome">Add</a>

@foreach (var income in Model)
{
    <div class="card" style="width: 18rem;">
        <h5 class="card-title">
            @income.IncomeAmount
        </h5>
        <div class="card-body">
            <p class="card-text">
                <div>@income.IncomeDate</div>
                <div>@income.IncomeSource</div>
            </p>
            <a class="btn btn-primary" href="Income/Income?incomeId=@income.IncomeId">View</a>
            <hr />
            <a class="btn btn-primary" href="Income/DeleteIncome?incomeId=@income.IncomeId">Delete</a>
        </div>
    </div>
}
@model Income
@if (Model.BudgetId != null && Model.BudgetId != 0)
{
    <a class="btn btn-primary" href="../Budget/Budget?budgetId=@Model.BudgetId">Back to Budget</a>
}

<div class="card" style="width: 18rem;">
    <h5 class="card-title">
        @Model.IncomeAmount
    </h5>
    <div class="card-body">
        <p class="card-text">
            <div>Received: @Model.IncomeDate</div>
            <div>From: @Model.IncomeSource</div> <!--Maybe put what budget it is a part of-->
        </p>
    </div>
</div>